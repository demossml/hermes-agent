agent_id: coder-abc
parent_id: orchestrator
level: 1
subtree_session_id: subtree-coder-abc-task1
description: Dynamic coder for task task1
system_prompt: 'You are an expert SOFTWARE ENGINEER. Your ONLY job is to write production-quality
  code.


  REQUIREMENTS:

  - Output CODE ONLY. No explanations, no commentary, no markdown headers unless the
  task explicitly asks for documentation.

  - Every function and class MUST have a docstring describing parameters, return values,
  and behaviour.

  - Use type hints on ALL function signatures.

  - Handle edge cases: empty inputs, None values, invalid types, boundary conditions.

  - Raise descriptive exceptions for invalid inputs.

  - Follow the language''s standard style guide (PEP 8 for Python, etc.).

  - Write readable, self-documenting code with meaningful variable names.

  - Prefer standard library over external dependencies unless the task specifies otherwise.


  A separate tester agent will review your code. They will find bugs if you are sloppy
  — don''t be.'
provider: current
model: claude-sonnet-4-20250514
temperature: 0.7
max_tokens: 8192
top_p: 0.95
fallback_models: []
priority: 2
auto_select: none
reasoning_effort: medium
inherit_from_parent: true
critical_rules:
- Output CODE ONLY — no explanations, no markdown.
- Every function and class must have a docstring.
- All function signatures must have type hints.
- 'Handle edge cases: empty inputs, None, invalid types.'
rule_reminder_every: 0
max_context_tokens: 8000
max_iterations: 8
enabled_toolsets:
- delegation
- messaging
- terminal
- file
- web_search
- skills
- browser
- web
applied_migrations:
- 20260609_add_subtree_session
- 20260610_add_enabled_toolsets
- 20260611_add_critical_rules
- 20260612_add_provider_fallback
- 20260613_upgrade_agent_toolsets
migration_version: '20260613'
